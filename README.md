# Median-data

Catalog data pipeline for the **median** Warframe companion. Builds a normalized
item catalog (SQLite, zstd-compressed) from the Warframe Public Export and
warframe.market, and publishes it as a GitHub Release that the median client
downloads.
